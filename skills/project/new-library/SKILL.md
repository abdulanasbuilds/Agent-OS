---
name: new-library
description: Start a reusable package or library with stable public APIs, compatibility goals, tests, documentation, versioning, and safe publishing boundaries.
---

# New Library

Route to `project-lifecycle` with profile `reusable library/package`.

Collect only decisions that materially affect language/runtime, public API, supported platforms, compatibility policy, test strategy, documentation, package registry, versioning, and release model.

Treat the public API as a contract. Avoid application-specific scaffolding and production secrets.
