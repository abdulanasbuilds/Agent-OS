# Dependency Adoption

Before adding a dependency, MCP server or skill ask: Is it necessary? Is it maintained? Is the source auditable? What permissions does it need? Does it execute code or transmit project data? Can versions be pinned? Are there known security concerns?

Prefer small, auditable dependencies over giant bundles.