---
name: new-cli
description: Start a command-line tool project with explicit command surface, input/output contracts, configuration, logging, tests, packaging, and distribution decisions.
---

# New CLI

Route to `project-lifecycle` with profile `cli/tool`.

Collect only decisions that materially affect language/runtime, command structure, config, filesystem behavior, interactive prompts, output formats, portability, and publishing.

Keep destructive commands explicit and confirmation-gated. Prefer deterministic, testable command behavior.
