---
name: new-web-app
description: Start a new web application through product intake, web-app UX, architecture, data/auth/security planning, workspace creation, and repository setup.
---

# New Web App

Thin alias for `project-lifecycle` with profile `web-app`.

Load the project lifecycle and web-app design route. Determine workflows, roles, navigation, data density, responsive behavior, auth, authorization, API/database needs, and deployment constraints before implementation.
