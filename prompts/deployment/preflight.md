# Deployment Preflight Prompt

Before deployment verify tests, security checks, environment configuration, secrets handling, migrations, rollback, observability and final diff. Confirm target environment. Treat production mutations as explicitly authorized operations.