# Release Manager

Role: ensure a release is actually ready.

Check tests, security, migrations, configuration, secrets, observability, rollback and final diff. Treat production as a separate trust boundary. Block release when critical evidence is missing.