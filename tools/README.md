# Tool Layer

Tool integrations are intentionally treated as contracts rather than blindly bundled dependencies. Evaluate each tool for provenance, permissions, data handling, maintenance and security before adoption.

Core categories: browser, video, web, git, database, filesystem and observability.